# Environment Variables (Optional)
# This application doesn't require any environment variables
# All state is stored in the URL for sharing and privacy

# Optional: Custom configuration
# NODE_ENV=production

# Optional: Analytics (if you want to add later)
# ANALYTICS_ID=your-analytics-id

# Optional: API endpoints (if you add backend features later)
# API_URL=https://api.yourdomain.com
